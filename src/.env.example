# Copy to ~/.claude/.env, fill in the real values, then `chmod 600 ~/.claude/.env`.
# Sourced by the `claude` shell wrapper (see README) and by ./setup.sh.
# Names only here — never commit values.

# Proxy. Leave empty when not behind one.
HTTP_PROXY=
HTTPS_PROXY=
NO_PROXY=

# GitLab — gitlab MCP, review-task-fetch hook. GITLAB_HOST is a bare host, no scheme.
GITLAB_HOST=
GITLAB_PAT=

# Grafana — grafana-dev MCP.
GRAFANA_URL=
GRAFANA_SERVICE_ACCOUNT_TOKEN=

# Docs and web search MCP.
CONTEXT7_API_KEY=
KEENABLE_API_KEY=

# Kubernetes — kubernetes-dev / kubernetes-stage MCP. Paths to kubeconfig files.
KUBECONFIG_DEV=
KUBECONFIG_STAGE=

# Jira — review-task-fetch hook. JIRA_URL is a bare host, no scheme.
JIRA_URL=
JIRA_PAT=

# Postgres — infra-mcp-postgres plugin.
PGPASSWORD_DEV=
PGPASSWORD_STAGE=
