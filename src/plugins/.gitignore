# Plugin cache (downloaded plugin files, auto-reinstalled)
cache/
# Plugin runtime data
data/
# Marketplace repos (auto-cloned from known_marketplaces.json)
marketplaces/
# Temp files
*.tmp
# Install counts cache
install-counts-cache.json
# Plugin in-use sweep marker (auto-generated)
.last_inuse_sweep
# Plugin catalog cache (auto-fetched, large)
plugin-catalog-cache.json
# Blocklist cache (auto-updated)
blocklist.json
# Plugin registry (runtime state: abs paths + timestamps, not portable)
installed_plugins.json
known_marketplaces.json
